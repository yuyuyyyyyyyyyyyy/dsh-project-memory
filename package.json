{
  "name": "dsh-project-memory",
  "version": "1.1.1",
  "type": "module",
  "description": "Cross-session project engineering memory for DSH coding agents: durable per-project records of problems, failed attempts, verified root causes and constraints, recalled automatically before the agent plans.",
  "main": "index.js",
  "exports": {
    ".": "./index.js"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --import ./register-deps.mjs test.mjs && node --import ./register-deps.mjs regression-consistency.test.mjs && node --import ./register-deps.mjs lease.test.mjs && node --import ./register-deps.mjs loader.test.mjs && node --import ./register-deps.mjs regression.test.mjs && node --import ./register-deps.mjs regression-forget.test.mjs",
    "pack:check": "npm pack --dry-run --ignore-scripts --json"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yuyuyyyyyyyyyyyy/dsh-project-memory.git"
  },
  "keywords": [
    "dsh",
    "dsh-plugin",
    "deepseek-harness",
    "coding-agent",
    "agent-memory",
    "context-engineering"
  ],
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "files": [
    "index.js",
    "cordis.patch.yml",
    "README.md",
    "LICENSE"
  ]
}
